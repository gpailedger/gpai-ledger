paths-ignore:
  # Archived third-party web pages: captured evidence, byte-exact by design.
  # Any key-like strings inside them are the page's own public client-side
  # config (e.g. Google Drive viewer keys), not repository credentials.
  - "data/captures/**"
